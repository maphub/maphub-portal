<% if not user_signed_in? %>


<h1>Welcome to maphub!</h1>
<div id="introduction"></div>

  <blockquote>
  maphub is a portal environment for social annotation of digitized (historic) maps, powered by YUMA.
  </blockquote>

  <div class="pseudoButton floatLeft"><%= link_to 'Log in', new_user_session_path %></div>
  <div class="pseudoButton floatLeft"><%= link_to 'Sign up', new_user_registration_path %></div>

<% else %>

  <h1>Welcome back to maphub!</h1>
  <div id="introduction"></div>        
  
  <h2>What's new</h2>
  <blockquote>
  maphub is a portal environment for social annotation of digitized (historic) maps, powered by YUMA.
  </blockquote>
  
  <div class="halfSize floatLeft">
    <h2>Recent Maps <small><%= link_to "(show all...)", maps_path %></small></h2>
    <%= render :partial => "maps/grid", :locals => { :maps => @recent_maps } %>
  </div>
  <div style="halfSize floatLeft">
    <h2>Recent Annotations <small><%= link_to "(show all...)", annotations_path %></small></h2>
    <%= render :partial => "annotations/recent" %>
  </div>
  
  <br style="clear:both">

<% end %>