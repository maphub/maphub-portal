<%
users = User.find(:all, :order => 'annotations_count', :limit => 10).reverse
%>
<ul>
<% for user in users %>  
  <% break if user.annotations_count.nil? %>
  <%= render :partial => "users/small_badge", :locals => { :user => user } %>
<% end %>
</ul>